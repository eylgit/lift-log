/**
 * The engine's data model. Filled in at B2.
 *
 * Nothing here may import from the shell — no React, no Dexie, no browser
 * globals. See tests/engine-purity.test.ts, which enforces that.
 */

export {};
